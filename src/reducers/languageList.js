const initialState = ["Vietnamese", "English", "Japanese"];

const myReducer = (state = initialState, action) => {
    switch(action.type){
        default:
            return state;
    }
}

export default myReducer;